

<div class="main-container">

 <div class="container">
  <div class="row align-items-start">
    <div class="col">

 <p><strong>Name:</strong>
    <%= @cocktail.name %>
    </p>

  <p><%= "#{@dose.description} #{@dose.ingredient}" %>
</p>

</div>
</div>
</div>





  <% @cocktail.doses.each do |dose| %>
    <p>
      <%= "#{dose.ingredient.name} #{dose.description}" %>
      <%= link_to 'Delete', dose_path(dose), method: :delete, data: { confirm: 'Sure?'} %>
    </p>
  <% end %>


 <hr>




 <div class="container">
  <div class="row align-items-start">
    <div class="col">
      <p><strong>Add doses to the cocktail:</strong></p>

      <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.input :description, placeholder: "describe your dose" %>
      <%= f.association :ingredient %>
      <%= f.submit "add dose", class: "btn btn-outline-light btn-lg" %>
      <% end %>
      </div>
    </div>
  </div>




  </div>
</div>
